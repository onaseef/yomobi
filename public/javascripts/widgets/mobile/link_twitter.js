//
// MOBILE
//
(function ($) {
  
  // 't' comes after 'b', thus I'm extending link_fb for lazily purposes
  window.widgetClasses.link_twitter = window.widgetClasses.link_fb.extend({
    baseName: 'twitter',
  });
  
})(jQuery);
