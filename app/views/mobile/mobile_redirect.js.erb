var exceptions = <%=raw @exceptions.to_json %>;
var isMobile = /<%= mobile_devices %>/i.test( navigator.userAgent.toLowerCase() );
var shouldRedirect = true;

for (var i=exceptions.length-1; i >=0; i--) {
  if (exceptions[i] === document.domain) {
    shouldRedirect = false;
    break;
  }
}

if (window._RSITE && isMobile && shouldRedirect) {
  window.location = window._RSITE;
}
